class Table {
    constructor(data){ 
        this.data = data
    }

    doSomething(){
        if(typeof this.data != 'undefined'){
            // console.log("Table got the data!")
        }
    }
}